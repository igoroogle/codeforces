program ZAD_D;

{$APPTYPE CONSOLE}

uses
  SysUtils;
var
   x1,y1,x2,y2,x3,y3:extended;
begin
   reset(input,'area1.in');
   rewrite(output,'area1.out');
   readln(x1,y1,x2,y2,x3,y3);
   writeln(abs((x2-x1)*(y3-y1)-(x3-x1)*(y2-y1))/2);
end.
