program ZAD_B;

{$APPTYPE CONSOLE}

uses
  SysUtils,Math;
var
   x1,y1,x2,y2:extended;
begin
   reset(input,'angle2.in');
   rewrite(output,'angle2.out');
   readln(x1,y1,x2,y2);
   writeln(arccos((x1*x2+y1*y2)/(sqrt(x1*x1+y1*y1)*sqrt(x2*x2+y2*y2))));
end.

