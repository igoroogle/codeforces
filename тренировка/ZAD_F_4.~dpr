program ZAD_F_4;

{$APPTYPE CONSOLE}

uses
  SysUtils;
var
   a,b,c:integer;
begin
  readln(a,b,c);
  if (c<b) and (trunc((a+b)/2*(b-a+1))>=c) then writeln('Take another envelope')
  else writeln('Stay with this envelope');
  readln;
end.
