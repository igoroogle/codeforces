#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
    int a, b, c, d, ans;
    cin >> a >> b >> c >> d;
    ans = a + b + c + d;
    cout << ((a == 1) && (b == 0) && (c == d)) << endl;
}
