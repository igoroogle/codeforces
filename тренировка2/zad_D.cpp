#include <string>
#include <cstdio>
#include <iostream>
#include <algorithm>
using namespace std;

#define x first
#define y second
#define pb push_back
#define mp make_pair
#define all(a) (a).begin(), (a).end()
#define sz(a) int((a).size())
#define sqr(a) ((a) * (a))
#define forn(i, n) for (int i = 0; i < int(n); i++)

typedef unsigned int unt;
typedef long long ll;
typedef long double ld;
typedef pair <int, int> pii;
typedef pair <ll, ll> pll;
typedef double geom;

const ll MOD = 457839487523;
const ll P = 768477;
const int INF = 2e9;
const long long INF64 = 9e18;
const long double EPS = 1e-9;
const long double PI = 3.14159265358979310000;
const int N = 300001;
const int M = 12001;

struct point
{
    ld x, y;
} a, b, c, d;

struct vect
{
    ld x, y;
};

ld s;

int main()
{
    //freopen("ragnarok.in", "r", stdin);
    //freopen("ragnarok.out", "w", stdout);
    cin >> s >> a.x >> a.y >> b.x >> b.y;
    ld aa, bb, cc, dd, l, r, m, m1, m2, g = sqrt(sqr(a.x - b.x) + sqr(a.y - b.y));
    l = 0;
    r = 10000000;
    while (r - l > EPS)
    {
        u
    }


    return 0;
}





