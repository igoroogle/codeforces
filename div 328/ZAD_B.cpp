#include <bits/stdc++.h>
#define sqr(a) (a) * (a)
using namespace std;
typedef long long ll;
typedef long double ld;
int main()
{
    ll n;
    cin >> n;
    cout << sqr(n - ll(2)) << endl;
    return 0;
}
